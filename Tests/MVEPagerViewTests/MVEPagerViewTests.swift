import XCTest
@testable import MVEPagerView

final class MVEPagerViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MVEPagerView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
